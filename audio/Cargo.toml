[package]
name = "nex-audio"
version = "0.1.0"
edition = "2021"
description = "Native audio sidecar for Nex: capture, Opus, playback."

[dependencies]
cpal = "0.15"
audiopus = "0.3.0-rc.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
